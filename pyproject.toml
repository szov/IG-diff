[project]
name = "ig-diff"
version = "0.1.0"
description = "Instagram follower diff: automated data export plus dashboard"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn",
    "loguru",
    "python-dotenv",
    "filelock",
    "playwright",
    "camoufox",
]
